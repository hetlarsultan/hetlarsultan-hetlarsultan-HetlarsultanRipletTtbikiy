import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aivideostudio.pro',
  appName: 'AI Video Studio Pro',
  webDir: 'dist'
};

export default config;
